# /models/__init__.py
# Training scripts and model utilities package.
# Phase 0: empty — training pipeline added in Phase 2.
#
# NOTE: Saved model artifacts (.pkl / .joblib / etc.) live in
# models/artifacts/ which is gitignored. Only training CODE is committed.
