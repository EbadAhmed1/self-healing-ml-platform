# .github/workflows/README.md
# GitHub Actions workflow directory.
#
# Phase 0: empty — no workflows yet.
#
# Planned workflows (added in later phases):
#   drift_check.yml     — scheduled daily drift detection (Phase 3)
#   retrain.yml         — triggered retraining on drift alert (Phase 3)
#   ci.yml              — PR checks: pytest + ruff + mypy (Phase 2)
